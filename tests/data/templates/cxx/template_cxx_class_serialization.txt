void ${ClassName}::Serialization(json_t& json)
{
    super::Serialization(json);

    ${ClassPropertiesSerialization}
}

void ${ClassName}::Deserialization(const json_t& json)
{
    super::Deserialization(json);

    ${ClassPropertiesDeserialization}
}
