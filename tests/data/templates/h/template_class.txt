EPI_GENREGION_BEGIN(${ClassName})

EPI_GENHIDDEN_${ClassName}()

public:
    constexpr static MetaTypeID TypeID{${ClassTypeID}};

    enum ${ClassName}_PIDs
    {
        ${ClassPIDs}
    };

${ClassPropertyCallbacks}
${ClassProperties}

EPI_GENREGION_END(${ClassName})